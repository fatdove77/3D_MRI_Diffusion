# --- Core Deep Learning Libraries ---
# 保持torch版本稳定，因为代码很可能依赖它
pytorch-lightning>=1.6.4,<1.8.0  # 给pip一些灵活性
torchio==0.18.80

# --- Data and Numerics ---
numpy
pandas
scikit-image
scikit-learn
SimpleITK

# --- MRI/NIfTI Handling ---
nibabel
nilearn
pydicom

# --- Utilities and Config ---
hydra-core==1.2.0
omegaconf==2.2.3
tqdm
einops
einops-exts
ema-pytorch

# --- Text/CLIP handling ---
ftfy
regex

transformers

# --- Visualization ---
matplotlib
imageio
imageio-ffmpeg
scikit-video


# ... (之前的所有库) ...

# --- Text/CLIP handling ---
ftfy
regex
transformers

git+https://github.com/openai/CLIP.git


rotary-embedding-torch

wandb
monai


# BioBERT相关依赖
transformers>=4.30.0  # HuggingFace Transformers库
tokenizers>=0.13.0    # 快速分词器
sentencepiece         # 某些模型可能需要

# 可选：如果要使用其他医学BERT变体
# scibert  # 如果使用SciBERT
# biobert-pytorch  # 另一个BioBERT实现

# 移除或标记为可选
# clip  # 不再必需，但可以保留作为备选
